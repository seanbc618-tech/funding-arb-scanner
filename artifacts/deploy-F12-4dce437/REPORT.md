# F01—F12 提交与部署（2026-09-09）

用户授权提交、部署、重启。运行代码提交 4dce437；发布 /home/sean/funding-arb/releases/f12-4dce437。

包含此前尚未提交的 F01—F12 及复核修补。Git 仅提交源码、检查、文档和服务模板；原始账户证据、凭据、运行账本未纳入。本次未推送远端 Git。

远端隔离发布目录 17 组、322 项检查通过。两份旧回归初次缺项目 PYTHONPATH，补齐测试环境路径后通过，无代码改动。运行文件、本地提交文件和发布文件哈希一致。

停止 reports timer，等待当次报告自然完成；停止监控/通知后逐文件原子替换代码，更新 reports service 的发布路径，再重启监控/通知。新快照和心跳更新后恢复 timer，显式运行报告服务验收成功。监控/通知 active/running，NRestarts=0；报告 Result=success；timer active/waiting。

backup/ 保存原代码和报告单元。回退仅回退代码和单元，不回滚或删除状态、账库。远端原有持仓/CSV/paper 文件哈希不变，本地 21 个原有文件哈希不变；数据库未复制、迁移或改写。正常监控快照和通知去重状态允许推进。

F08—F12 paper 代码已安装，但没有初始化模拟资金、启动 paper_loop 或创建自动执行服务。真实交易未启用，未下单、撤单、划转或改变账户模式。F13 未开始。部署完成不等于 F15/F16 无人值守交易验收。

详细服务/时间/哈希结果见 verification.json 与 manifest.json。
