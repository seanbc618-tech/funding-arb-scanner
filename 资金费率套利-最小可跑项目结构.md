# 资金费率套利 · 最小可跑项目结构

> 目标：一套代码、配置切换三种模式  
> ① 同所现货↔永续　② 跨所期现/期期　③ 跨所费率差  
> 建议上线顺序：① → ② → ③

---

## 0. 一句话定位

用 **现货/永续对冲** 锁定方向风险，主要吃 **funding（资金费率）** 和部分 **basis（基差）**，不是赌涨跌。

---

## 1. 交易所选型（起步）

| 优先级 | 交易所 | 用途建议 | 理由 |
|--------|--------|----------|------|
| P0 | Binance | ① 同所主力 | 流动性最好、API 成熟、现货+U本位永续齐全 |
| P0 | Bybit | ① 或 ② 对腿 | 费率波动常见、API 稳定、适合做第二腿 |
| P1 | OKX | ②③ 扩展 | 费率差机会多，接口略繁 |
| 暂缓 | 小所 / 高费率野所 | — | 提现慢、假量、API 断联，后期再碰 |

**起步组合推荐**

- **只做 ①**：先选 **Binance 单所**（现货 + USDT 永续）跑通全链路。
- **上 ②③**：再加 **Bybit**，形成 Binance ↔ Bybit。
- 账户：现货账户 + 合约账户（或统一账户）；API Key 只开交易、**禁止提现**。

**品种起步池（少而精）**

- 先做：`BTCUSDT`、`ETHUSDT`（深度好、费率相对规律）
- 再扩：高费率山寨（机会大，滑点/爆仓/下架风险也大）
- 规则：单币种净敞口 ≈ 0；总名义暴露设硬顶

---

## 2. 三种模式（同一 bot，配置切换）

### 模式 A · 同所现货↔永续（①，先做）

```
买入现货 + 开空永续   →  吃正 funding（空头收费率）
卖出现货 + 开多永续   →  吃负 funding（多头收费率）  ※需可借券或已有现货
```

- 腿数：2（同所）
- 关键点：资金占用、费率翻向、现货卖出受限（负费率场景）
- 成功标准：能自动开仓对冲、定时收 funding、费率低于阈值自动平仓

### 模式 B · 跨所期现 / 期期（②）

```
例：Binance 买现货 + Bybit 开空永续
或：Binance 永续多 + Bybit 永续空（两边费率/基差差）
```

- 腿数：2（跨所）
- 痛点：两边同时成交失败、划转/余额不足、一端断联
- 成功标准：原子化开平（要么两边都成，要么回滚）、延迟与滑点记账

### 模式 C · 跨所费率差（③）

```
同一币：A 所 funding 很高开空，B 所很低/反向开多，名义对冲
```

- 腿数：2（两所永续）
- 痛点：费率突变两边一起亏、强平、维持保证金
- 成功标准：实时双边费率、预期净收益 > 成本阈值才开

---

## 3. 目录与模块清单（建议）

```
funding-arb/
├── README.md                 # 本文件可拆成项目说明
├── .env.example              # API Key 模板（勿提交真密钥）
├── config/
│   ├── exchanges.yaml        # 所、账户、费率、杠杆上限
│   ├── symbols.yaml          # 交易对白名单
│   └── modes.yaml            # A/B/C 模式开关与阈值
├── src/
│   ├── main.py               # 入口：加载配置 → 选模式 → 跑循环
│   ├── exchange/             # 交易所适配层
│   │   ├── base.py           # 统一接口：行情/下单/仓位/余额
│   │   ├── binance.py
│   │   └── bybit.py
│   ├── market/               # 行情
│   │   ├── funding.py        # funding rate + 下次结算时间
│   │   ├── basis.py          # 现货-永续价差
│   │   └── orderbook.py      # 深度 / 可成交量估算
│   ├── strategy/             # 策略
│   │   ├── mode_a_spot_perp.py
│   │   ├── mode_b_cross.py
│   │   └── mode_c_funding_diff.py
│   ├── execution/            # 执行
│   │   ├── hedge.py          # 对冲开平（两腿协调）
│   │   ├── order.py          # 限价/市价、重试、部分成交
│   │   └── reconcile.py      # 对账：期望仓位 vs 实际
│   ├── risk/                 # 风控（硬门禁）
│   │   ├── limits.py         # 名义上限、杠杆、单币上限
│   │   ├── kill_switch.py    # 一键平仓 / 停机
│   │   └── health.py         # API 延迟、余额、保证金率
│   ├── accounting/           # 记账
│   │   ├── pnl.py            # funding 收入、手续费、滑点、净利
│   │   └── ledger.py         # 本地流水
│   └── notify/               # 告警（Telegram / 邮件 / 本地 log）
├── scripts/
│   ├── paper_run.py          # 模拟盘 / 只读扫描
│   └── flat_all.py           # 紧急全平
└── data/                     # 本地库或 CSV（gitignore）
```

### 模块职责一句话

| 模块 | 做什么 |
|------|--------|
| `exchange/` | 屏蔽各所 API 差异，统一下单与查仓 |
| `market/funding` | 拉当前/预测 funding、结算倒计时 |
| `market/basis` | 算期现价差是否够覆盖成本 |
| `strategy/*` | 只决定「该不该开、开多大」，不下单细节 |
| `execution/hedge` | 保证两腿尽量同时到位，失败则减腿回滚 |
| `risk/*` | 任何策略之前的硬拦截 |
| `accounting/*` | 每笔 funding、手续费、净 APR 可追溯 |
| `notify` | 开平仓、异常、保证金告警 |

---

## 4. 核心配置项（`modes.yaml` 示意）

```yaml
mode: A   # A | B | C

common:
  symbols: [BTCUSDT, ETHUSDT]
  min_net_funding_apr: 0.15      # 年化预期低于此不开（示例，按你成本改）
  max_notional_usd: 5000         # 总名义硬顶
  max_leverage: 2                # 永续杠杆上限（宁低勿高）
  open_slippage_bps: 5           # 开仓可接受滑点
  close_when_funding_flip: true  # 费率翻向则平
  settle_buffer_minutes: 10      # 结算前 N 分钟禁新开

mode_a:
  exchange: binance
  spot_leg: buy
  perp_leg: short

mode_b:
  spot_exchange: binance
  perp_exchange: bybit
  # 或两边都是 perp

mode_c:
  long_funding_exchange: bybit   # 去费率更低/更负的一边做多
  short_funding_exchange: binance
  min_funding_spread: 0.0003     # 单期费率差阈值（示例）
```

---

## 5. 主循环（伪流程）

```
每 N 秒 / 或结算前扫描：
  1. 健康检查（API、余额、保证金）→ 不过则告警并跳过
  2. 拉 funding + basis + 盘口可成交量
  3. 估算：预期收入 - 手续费 - 滑点 - 划转成本 = 净期望
  4. 净期望 < 阈值 → 只记录，不开
  5. 过风控硬顶 → hedge 开仓（两腿）
  6. 持仓中：监控费率翻向、保证金、单腿敞口漂移 → 触发平仓/减仓
  7. 结算后记账 funding，更新 PnL
```

**纸面先跑**：`paper_run` 只扫描和记「若开仓会怎样」，连续观察若干个 funding 周期再上真钱小仓。

---

## 6. 风控硬清单（上真钱前必须有）

- [ ] API Key **无提现**权限  
- [ ] 总名义 / 单币名义硬顶  
- [ ] 永续杠杆 ≤ 2～3（学习期）  
- [ ] 单腿失败 → 自动减腿或市价回滚  
- [ ] 保证金率告警 + 自动减仓  
- [ ] 进程挂掉可重启且能 `reconcile` 对账  
- [ ] `flat_all` 紧急全平脚本可手动跑  
- [ ] 日志与 funding 流水落盘，能算出真实净 APR  

---

## 7. 分阶段里程碑

### Phase 1 · 模式 A（同所）— 1～2 周量级

- Binance 适配：现货买卖、U 永续开平、查 funding  
- 纸面扫描 → 小名义实盘  
- 完整记账：funding、手续费、滑点  

### Phase 2 · 模式 B（跨所）

- 加入 Bybit  
- 对冲执行器：两腿超时、部分成交、回滚  
- 余额预留策略（两边都留保证金，少做临时划转）  

### Phase 3 · 模式 C（费率差）

- 双边 funding 矩阵扫描  
- 更严的开仓阈值与持仓时长（常只拿到下一两期结算）  
- 压力测试：费率骤变、一端停机  

---

## 8. 成本与「别幻觉」

净利润粗算：

```
净利 ≈ Σ funding 收入
      - 开平手续费（×2 腿 ×2 次）
      - 滑点
      - 划转/链上成本（若有）
      - 资金占用机会成本
```

费率看起来「年化 50%」很常见是瞬时年化；真实可抓窗口短、还会翻向。以 **记账后的净 APR** 为准，不要以交易所展示的 funding APR 当收益。

---

## 9. 和「搬砖」的区别

| | 资金费率套利 | 经典跨所搬砖 |
|--|-------------|--------------|
| 利润来源 | funding / 基差 | 两所现货价差 |
| 方向风险 | 对冲后接近中性 | 转账途中有敞口 |
| 速度要求 | 中（结算周期小时级） | 高（秒级价差消失） |
| 资金 | 双边占用 | 双边库存或快速划转 |

---

## 10. 下一步可选项

1. 把本结构 scaffold 成空仓库（目录 + 接口 stub）  
2. 先做 **只读扫描器**：输出「当前哪些币 funding 值得看」CSV  
3. 指定只用 Binance 模式 A，写一版 `config` + 伪代码级 `main` 流程  

---

*文档版本：2026-09-05 · 雄魂研报向 · 最小可跑结构，非投资建议*
