# 交接文档 · 资金费率套利

## 2026-09-09 F13 已部署启用（最新状态）

- 提交 90c64d5，发布 releases/f13-90c64d5，远端 338 项检查通过。仅更新 paper_loop.py/exit_a.py，paper 服务新增 --auto-exit 并重启。
- BTC/ETH 模拟仓位均 HOLD，无 pending；新状态含 EXIT_CHECKED，扫描和持仓检查新鲜。资金、额度、无最长持有时限保持。
- 切换期间账本字节不变，原代码/单元/停止时账本留存于发布 backup/；不得回滚运行账本。未真实交易，本轮未推送 Git。
- artifacts/deploy-F13-90c64d5/REPORT.md 与 verification.json 为部署证据。F14 未开始。

## 2026-09-09 F13 复核完成（最新状态）

- 修复两腿已确认归零但阶段写入中断后无法收尾、多币退出混合结果误报完成两项。未知订单/缺历史仍阻断。
- 全套 338 项检查通过，21 个原有文件不变；未部署、未修改线上持仓。线上仍没有 F13 自动退出，F14 未开始。
- 证据 artifacts/F13-review/REPORT.md、before.log、f13_exit.log、verification.json。

## 2026-09-09 F13 本地完成（最新状态）

- exit_a.py 与 paper_loop.py --auto-exit 已实现自动持有/退出；风险敞口、明确退出续做、负费率、可选持有时限依次优先。默认关闭，缺口和未知订单阻断。
- 334 项检查通过；已确认部分退出可在重新检查后续做。真实保证金/强平模型未套用到 paper。
- 未部署、未重启或改变线上 BTC/ETH 模拟仓位；线上仍无 F13 自动退出。F14 未开始，F13 待审阅。
- 证据 artifacts/F13/REPORT.md、exit-example.json、verification.json。

## 2026-09-09 paper 已启动（最新状态）

- 用户授权模拟本金 20000 USDT、单腿 2000、组合双腿名义上限 10000。BTC/ETH，最多 2 币，单币双腿上限 5000，备用金 1000；持有场景 720 小时、压力 50 bps。
- funding-arb-paper.service active/running，未开机自启、Restart=no；扫描和持仓检查推进。最新已建立 ETH 0.802 与 BTC 0.0253 基础币的模拟对冲仓位，pending=null。没有真实订单。
- 启动时 BTC 超预留预算零成交却标 needs_close，已修复为确认零成交关闭；原空周期使用既有 close 路径关闭，ETH/现金保留，随后新扫描正常开 BTC。
- 补丁提交并推送 84e8cf4；远端增量发布 releases/paper-84e8cf4（根目录仅 paper_a.py 替换，其他模块仍对应 f12-4dce437）。322 项本地检查、17 项远端针对检查通过。
- 自动退出 F13 未完成；退出建议仅阻止新增，需要人工处理。证据 artifacts/paper-start-20260909/verification.json，远端修复前 paper 状态备份在增量发布目录内。

## 2026-09-09 F01–F12 已提交部署并重启（最新状态）

- 用户授权提交、部署、重启；运行代码提交 4dce437，新发布 releases/f12-4dce437。此前未部署声明为历史记录。
- 远端 322 项检查通过；运行/发布/本地源码哈希一致；监控和通知已重启且新鲜，报告运行成功，定时器已恢复。
- 状态与账库保留；paper 代码已安装但未启动主循环，未真实交易；F13 未开始。Git 未推送。
- 备份在新发布 backup/；部署报告 artifacts/deploy-F12-4dce437/REPORT.md。

## 2026-09-09 F10–F12 联合复核（最新状态）

- 复现并修复 4 项：排名量被 ticker 改写、结算边界旧排名放行、持仓 HOLD 检查过期仍放行、整批预留 owner 未原子持久化。
- 6 个复核用例与 316 个既有用例，共 322 项通过；21 个原有状态/研究文件未变。
- 修补仅本地、未联网重跑、未部署或真实交易，F13 未开始。报告与修复前证据见 artifacts/F10-F12-review/REPORT.md。

## 2026-09-09 F12 本地完成（最新状态）

- paper_loop.py 已串联持仓检查、账本对账、缓存资金费、排名、预留和 paper 执行。扫描独立进程，主循环非阻塞读取；持仓/成交变化要求重扫。
- 316 项检查通过，独立进程离线实验通过；21 个原有状态/研究文件未变。未常驻运行、未联网验收、未部署或真实交易。
- 默认一轮，持续运行需 --cycles 0；退出建议仅记录，F13 未开始。网络请求本身仍可能延迟，不是实时系统。
- artifacts/F12/REPORT.md、process-smoke.json、verification.json 为验收证据；F12 待审阅。

## 2026-09-09 F11 本地完成（最新状态）

- allocate_a.py 已实现 paper 单币/总双腿名义、持仓数和备用金限制，预留/执行/释放接入 paper_a.py；已知部分成交保留实际占用，未知执行不重放。
- 17 项专项加 284 项既有，共 301 项通过；21 个原有状态/研究文件未变。本轮离线合成验收，未操作 VPS、未部署、未真实交易。
- 预留/报价有效期 5 秒；内部函数须在同一账本锁内调用，F12 再串联自动循环。F11 待审阅，F12 未开始。
- artifacts/F11/REPORT.md、partial-example.json 和 verification.json 为交付证据。

## 2026-09-09 F10 本地完成（最新状态）

- 新增 rank_a.py：账户费率、按金额四笔盘口、完整历史/当前预测分离、整数结算窗口、总资金收益率排名、回本和基差压力。284 项检查通过，21 个原有状态/研究文件未变。
- BTC/ETH 真实只读费率和行情链路通过，最终源码哈希一致；30 天场景两者预计净收益很薄，50 bps 压力下均为负。不是实际收益或执行许可。
- F10 未部署、未真实交易；没有修改旧扫描 CSV 或 paper 状态。F11 未开始，待审阅。证据 artifacts/F10/REPORT.md、live-preview.json、verification.json。

## 2026-09-09 F09 复核与联网补验完成（最新状态）

- 用户加载 SSH 密钥后认证恢复；F09 公共费率/标记价格接口及合成持仓记账、重启去重通过，远端验证源码哈希与本地一致。
- 本轮未发现需修复的实现缺陷；新增 4 个边界检查，全套 268 项通过。原有 21 个状态/研究文件未变。
- 未部署或真实交易；此前 F09 联网待验状态已由本次验证补齐。F10 未开始。
- 证据与边界：artifacts/F09-review/REPORT.md、public-validation.json、verification.json。线上服务健康未在本轮重新验收。

## 2026-09-09 F09 本地交付（最新状态）

- 新增 paper_funding.py 与 paper_a.py settle；新成交记录执行毫秒时间，按指定 fundingTime 回放历史持仓、计模拟支付、原子去重。264 项离线检查通过。
- 缺真实已结算费率、目标分钟标记价格或历史成交时间，记 INVALID_DECLARED_GAP/null，不改现金或填造数据；分钟开盘价是显式模拟计价口径。
- VPS SSH 认证失败、本机公共接口超时；联网验收未通过，未部署、未真实交易。线上版本未重新核验，不以此前记录代替当前健康证据。
- 原有 21 个状态/研究文件不变。F09 待审阅及联网补验，F10 未开始。详情 artifacts/F09/REPORT.md。

## 2026-09-09 F08 复核完成（最新状态）

- 复现并修复 2 类问题：限价取整回退丢失有效深度；平仓资金预留遗漏出售所得/保证金释放/实际亏损。
- 4 个新增用例修复前失败，修复后通过；全套 244 项通过，21 个原有状态/研究文件未变。
- 本轮未联网重跑、未部署、未真实交易；当前线上仍为 F01–F07。F09 未开始。
- 证据、差异与具体算例见 artifacts/F08-review/REPORT.md；上轮公共盘口证据只对应当时源码。

## 2026-09-09 F08 本地完成（最新状态）

- 用户批准 F08，独立 paper_a.py 已完成；240 项检查通过，21 个原有状态/研究文件哈希不变。
- VPS 临时环境使用公共真实盘口完成 BTC 模拟开平仓，证据 artifacts/F08/public-preview.json；没有真实订单、部署或重启服务。
- 显式模拟资金/费率、盘口深度与精度、部分成交、持久预留及单腿失败保护已实现。pending 保留并阻止自动重放。
- 当前线上仍为下方 F01–F07 发布。F08 仅本地交付，待审阅；F09 未开始。详情 artifacts/F08/REPORT.md。

## 2026-09-09 F01–F07 更新已全部部署（最新状态）

- 用户授权全部更新上线，F01–F07 及两轮复核修补已部署至 releases/f01-f07-20260909-586bd8ed1c65；原运行目录模块同步更新，F06 服务指向新发布目录。
- VPS 224 项离线检查通过，新版真实账户只读预检通过。运行文件/发布文件/单元哈希一致，监控和通知 active/running，timer active/waiting；事件回执 3660，去重状态正常。
- 持仓与账库保留，现有双向模式及外部仓位不变。F07 仅安装代码，没有运行交易命令或新增自动交易服务。
- backup/ 保存原代码与单元；不回滚状态/账库。详细验收见 artifacts/deploy-F01-F07-20260909/REPORT.md 和 verification.json。
- 下方各阶段“未部署/待部署”为历史状态，已被本次部署记录覆盖。F08 未开始。


## 2026-09-09 F07 复核（优先于下方交付）

- 发现并本地修复 2 项：管理器旧退出判断可能误平同币新周期；客户订单号去重遗漏 previous 历史链。
- 管理器传 expected_trade_id，do_close 持锁核对，缺失 ID 不能绕过；历史订单号纳入提交前去重。无 pending 的 paused 显示验证正常，不计缺陷。
- 新增 4 个复核用例，加原 220 个，共 224 项通过。报告、复现前后日志、源码哈希与局部差异见 artifacts/F07-review/。
- 21 个原有状态/研究文件不变。F07 与本轮修补未部署、未实盘；此前 F01–F06 只读补丁也仍待独立部署。F08 未开始。


## 2026-09-09 F07 执行状态与单账户控制（最新事实）

- 用户批准 F07，本地实施完成：交易 ID/目标/确认余量/费用/敞口、已知和未知订单状态、按账户绑定的执行锁、全局启动门槛。22 专项＋198 既有检查，共 220 项通过。
- 开/平/recover 函数入口统一受锁保护；同机同用户各 checkout 共享账户锁并绑定唯一状态文件路径。跨主机/其他用户/旧入口不在保证范围内。
- 未知原单只查询、不重发；其他币种 pending 或未完成阶段阻止新开仓；旧无版本状态仅可读，不自动迁移。状态文件与锁权限 0600。
- 当前双向账户及现有仓位保持原样；没有调用真实执行器、部署、交易、重启或手动发消息。F01–F06 复核的只读修补仍待独立部署，勿混入 F07 执行器。
- 报告、最终源码哈希、局部差异和离线样例见 artifacts/F07/。F07 待审阅，F08 未开始。


## 2026-09-09 F01–F06 复核断点（优先于下方历史记录）

- 用户要求复核 F01–F06，现已收尾；发现 5 处代码问题，本地修复、198 项检查通过，**尚未部署这些修补**。F07 未开始。
- 修复：外部仓位变化漏事件、全账户覆盖证据无法供持仓报表复用、F04 成交订单与合约腿未交叉验证、超时估值分项未清空、查询失败误报保证金零。
- 两份旧回归脚本从临时目录丢失，已从原始文件变更记录恢复到 checks/funding_arb_regression.py、checks/funding_modules_regression.py；24+29 项现可从项目重跑。
- 用户因现有仓位暂不能切换单向。保留账户与仓位；本地与远端门禁模拟验证均拒绝 long_short_mode，当前只有只读监控/报表。
- 最新线上只读检查服务健康；自然日报回执 3645、最近小时摘要 3651 均确认，数据仍有缺口。未重启、部署或手动发送消息。
- 下次先核验/部署本轮只读修补；不要误把本地修复当成已上线，也不要覆盖交易状态。报告、源码哈希、复现和验证见 artifacts/F01-F06-review/REPORT.md 与 verification.json。


## 2026-09-09 F06 定时报告与独立检查（最新事实）

- F05 已获准继续，F06 已实施并上线，待审阅；F07 未开始。
- 每小时摘要、北京时间 08:00 日报；独立 timer 每分钟检查监控/通知服务及快照/心跳，陈旧阈值 180 秒。异常变化冷却 300 秒，恢复通知，不重复同一状态或时段。
- 新发布 releases/f06-20260909-3e35de2dcbec；新增 funding-arb-reports.timer/service，原通知模块增加心跳。监控未重启，原运行目录交易/账目/风险模块未替换。
- 25 专项＋163 既有检查通过；真实摘要回执 3633，重复触发状态不变。保护文件/账库不变，未交易、划转、改模式、提交或推送。
- 账户仍 long_short_mode，外部仓位 1、本地策略记录 0；旧 F04 库缺日报完整覆盖，入账字段 null；日级策略净收益缺完整归属/估值证据亦 null。
- 同机 watchdog 无法检测整机/systemd/网络整体失联；正常的 oneshot 服务完成后显示 inactive/dead，以 timer active 和服务 Result=success 为准。详情见 artifacts/F06/REPORT.md。

以下为各阶段的历史交付记录，状态以本节为准。

## 2026-09-09 F05 只读收益展示（当前事实）

- F05 已实现，待审阅；portfolio_a.py 读取 F04 库和账户，输出 JSON/Markdown，区分真实入账、已实现、未实现和预计退出成本。
- 21 个专项＋142 个既有回归通过，原有本地文件与远端 F04 数据库未变。账户净资产为 USD，策略资本未归属时净值与收益率保持 null。
- 远端独立验证副本/报表位于 evidence/f05-20260909，未替换运行模块；最终 portfolio_final.py 与 report-final.json/report-final.md 对应最终实现。
- 最新只读仍为 long_short_mode，外部仓位 1，本策略 live 状态无记录；没有修改模式或接管外部仓位。
- 无订单/撤单/划转、额外 Telegram、Git 提交/推送；证据见 artifacts/F05/REPORT.md，下一项 F06 尚未开始。

## 2026-09-09 F04 只读采集与账户事实（优先于历史记录）

- OKX 凭据现已生效：配置从 root 目录安全复制至服务用户目录，监控仍只读。
- 账户 Futures mode acctLv=2、long_short_mode；现有逐仓多头不归属策略。账户汇总 mgnRatio 为空、仓位级有值；不能用该缺口宣称保证金不足，也不能放行净持仓模式执行器。
- F04 新增持久分页/覆盖证据、重叠复查、更正冲突、账户级只读连续采集入口。18 个专项＋124 个回归通过，待审阅。
- 真实最近 24h 两次只读采集各 1 条账单，无重复入库；策略收益为 null。远端独立验证副本 accounting_f04_review_20260909.py，独立证据库 evidence/f04-20260909/account.sqlite（600）。未替换运行 accounting.py 或启动采集服务。
- 详细证据与限制见 artifacts/F04/REPORT.md。无交易、账户模式变更、Git 提交/推送；下一项 F05 尚未开始。

## 2026-09-09 F03 已部署（当前事实，优先于历史记录）

- F02 获准继续；F03 独立 Telegram 事件服务已完成，待审阅。notify_events.py 读取快照，300 秒节流、持久去重、不重发未知投递。
- 远端向“情报小分队”发送实际 CONFIG_MISSING 告警，消息 3629 确认；重启通知服务后不重发。监控与通知服务 active/enabled，缺 OKX 凭据仍不能证明无仓。
- 共 124 个离线用例通过；原有远端 3 份状态文件与本地 21 份状态/研究文件未变。
- 本轮部署 monitor_a.py、notify_events.py、notify_run.py、check_a.py 及通知服务；私有通知配置单独保存为 600 权限。没有交易、OKX 账户调用或 Git 提交/推送。
- 注意：trade_a.py 的本地 F02 复审修复未部署；目前仅运行只读监控与通知，未来部署执行器需带入最新修复。
- 证据与限制见 artifacts/F03/REPORT.md。F04 尚未开始。

## 2026-09-09 F02 复审修复（当前事实，未部署）

- 额外复现并修复 trade_a.account 将未知余额当零、null 挂单当无挂单的问题；共用账户门禁现在校验明确数值与列表响应。
- F02 34＋F01 18＋既有 53，共 105 个离线用例通过，21 个状态/研究文件未变。
- 前次 99 个测试未覆盖上述漏洞；以 artifacts/F02/REPORT.md 末尾复审更正及 review-* 证据为准。
- 未访问真实账户、未交易或部署；F02 待审阅，F03 未开始。

## F02 本地交付（2026-09-08，优先于下方历史记录；尚未部署）

- F01 已由用户审阅；复核未发现新的阻塞问题。F02 独立风险评估已完成，待用户审阅。
- check_a.py 分别输出 risk、execution、data_status，盘口/接口失败不再隐藏可取得的保证金与清算风险。
- 顶层 action 仍保守：即使 risk=EXIT_REQUIRED，只要执行受阻或数据有缺口，action=BLOCK，管理器不会平仓。
- 28 个 F02 专项、18 个 F01 回归与既有 53 个离线回归通过；21 个历史状态/研究文件哈希不变。
- 说明与证据见 artifacts/F02/REPORT.md。没有账户调用、真实订单、通知、提交/推送或部署；下一项 F03 尚未开始。

## F01 本地交付（2026-09-08，尚未部署）

- 按已审阅实施方案完成监控对账修正：方向、张数、现货 baseline + base、关闭记录、未知值、未归属与重复仓位。
- `python3 checks/f01_reconciliation.py`：18 个专项验收通过；既有 53 个离线回归通过。
- 不修改历史状态、不访问真实账户、不产生真实订单；远端仍需独立授权及验证，不能把本地修复视为已上线。
- F01 待用户审阅；按方案逐项停止，F02 尚未实施。细节见实施方案末尾 F01 交付记录。


## Telegram Notifier 接入

- 已使用用户插件向“情报小分队”发送一次 VPS 只读状态简报。
- 新增 `notify_run.py` 复用插件 CLI，业务命令结束后发送白名单摘要，通知失败不重跑业务或重发消息。
  配置与 Token 留在插件私有配置中，不复制入库；用法见 README。
- `monitor_a.py` 和 `funding-arb-monitor.service` 提供只读常驻持仓监控；不带 `--live`，只写状态快照和用户日志。
  Telegram 插件仍只在本机按次调用，Token 未复制到仓库或 VPS。

## 模式 A 功能补齐（本轮更新，优先于下文）

- 新增 `accounting.py`：只读成交/资金费率账单采集，SQLite 原始证据去重及冲突检查，
  按本地订单归属核对成交量和费用；平仓后输出查询时点的净损益，缺口为 null。
  超过保守 89 天供应商窗口直接 INVALID_DECLARED_GAP，不修改旧状态或补造历史。
- 新增 `check_a.py`：订单簿数量/VWAP/时效、基差、可用资金、保证金率、清算距离和净敞口。
- 新增 `manage_a.py`：单次只读退出建议；`--account` 只读真实账户，只有 `--live` 才执行 EXIT。
  用户选定保守默认：资料不全暂停，费率负值、保证金/清算距离越线建议退出。
  具体阈值和 CLI 见 README；无自动开仓、资金划转、杠杆修改或新增交易定时任务。
- `trade_a.py` 接入开仓检查，真实订单改为带限价的 IOC，平仓仍 reduceOnly；
  每笔订单保存盘口依据，成交按原订单核验，未完成余量持续保留。
- `monitor_a.py` 轮询 OKX 账户仓位、余额和保证金资料，与本地实盘状态比对；缺凭据、缺清算数据、
  API 异常和未归属仓位都保留为显式状态，不把它们折叠成“无仓”。
- 会计只提供策略归属损益；总资本收益率不补造。供应商迟发账单仍需复查。
- 本轮仅本地离线验证：原有 24 个及新增 29 个用例共 53 个通过。
  临时脚本：`funding_arb_regression.py`、`funding_modules_regression.py`（系统临时目录）。
  未连接真实账户、未下单、未部署；下方旧版市场单说明已由 IOC 取代。

## 2026-09-08 接手更新（优先于下方历史快照）

- `trade_a.py` 已隔离 dry/live 状态、检查记录模式、原子落盘并加同模式进程锁。
  旧非空 `positions_a.json` 在实盘开/平仓和 recover 前必须人工核对迁移，原文件不自动修改；空跑与只读检查不会读取或修改它。
- 下单前保存唯一客户订单号；按原单查询终态、实际成交与手续费，再更新余量。
  超时/查不到/仍挂单时保留 pending，不盲目回滚或重发。`recover COIN --live` 仅查询核对，
  `close COIN --live` 按确认余量退出；永续平仓 reduceOnly，成交流水保留。
- 账户支持边界为 Futures mode (`acctLv=2`) + net_mode + cross 永续/cash 现货，
  不自动改模式、杠杆或划转。余额/仓位不匹配时停止；不支持其它程序并发操作同币账户资产。
- `trade_a.py candidates` 是 OKX 模式 A 的公共行情入口。`pool.py` 仍是模式 C 研究，
  暂缓定时运行；缺数据保留旧空跑仓位，输出待核对，不伪造平仓收益。
- 统一数据门槛：量缺失不放行、历史不足五天/陈旧/内部缺口不入候选，缺口留 null 和原因。
  verify 按整周检查，并保留 symbol 防止多合约覆盖。旧 CSV 和状态不重写。
- 所谓“净 APR”统一解释为“扣手续费后的名义年化估算”，兼容保留旧字段名。
  未计滑点、基差变化、融资费用和总资金占用；没有资金费率入账采集和完整账户收益会计。
- 下一步是配置私有账户环境文件后做账户只读核对；历史中的“小额实盘下一步”不是自动执行授权。
  本次修改没有下单、撤单或划转；远端只启用了只读监控服务，没有安装交易 cron。
- 远端用户级 systemd 的 `funding-arb-monitor.service` 当前为 enabled/active，用户 linger 已启用。
  因 OKX 凭据尚未配置，最新快照为 `CONFIG_MISSING`，不是“无仓位”结论。
- 使用方式和恢复边界以 README 为准。下文 2026-09-07 的费率、VPS、mock 描述是历史快照，
  不代表当前行情或本次独立验收，不能据此宣称当前代码已实盘验证。
- 本次离线验证：Python 3.13.7 / ccxt 4.5.77，24 个临时 unittest 用例通过；
  包含请求参数生成、模式隔离、响应丢失、部分成交、恢复幂等、手续费、账户漂移、
  数据缺口和整周覆盖。四脚本 AST 与 `git diff --check` 通过。
  临时回归脚本在 `/private/tmp/funding_arb_regression.py`，未增加仓库测试依赖。

## 2026-09-07 历史交接快照

> 面向接手的人/AI。读完这份应该知道：项目走到哪了、为什么这么选、哪些坑别再踩。
> 仓库：github.com/seanbc618-tech/funding-arb-scanner

## 一句话现状

四个脚本，全部只读或空跑，**没有下过一笔真单**。策略方向已确定：
**模式 A（同所买现货 + 开空永续，吃资金费率），首选 okx**。
下一步是用小额实盘跑通开仓→收费率→平仓的完整链路。

---

## 文件

| 文件 | 作用 | 状态 |
|------|------|------|
| `scan.py` | 扫 16 家 CEX/DEX 的永续资金费率，输出三张表：单所费率榜、跨所费率差、模式 A 候选 | 跑通，数据可信 |
| `verify.py` | 拉 28 天历史按周切片，验证费率偏差是不是稳定的（不是一天的噪音） | 跑通 |
| `pool.py` | 池子管理：自动配对跨所机会、价差压缩就换仓。复用 `scan.py` 的抓取 | 跑通，空跑 |
| `trade_a.py` | 模式 A 执行器（okx）：开仓/平仓/查仓 | **只验过 mock，没碰过真账户** |
| `monitor_a.py` | 只读轮询本地实盘状态与 OKX 账户，写状态快照和日志 | 只读，缺凭据显式报错 |
| `funding-arb-monitor.service` | 用户级 systemd 常驻监控单元 | 不带 `--live`，远端已启用 |

其余：`README.md`（对外说明 + 坑清单）、`LICENSE`（MIT）、`HANDOFF.md`（本文件）。
运行产物 `funding_*.csv` / `spread_*.csv` / `verify_*.csv` / `positions*.json` / `pool_log.csv` 都在 `.gitignore` 里。

## 运行环境

- VPS 连接地址与登录用户名不在公开仓库记录，由项目所有者私下提供。代码在 `~/funding-arb/`，venv 在 `.venv/`
- 依赖只有 **ccxt** 一个。Python 3。
- VPS 直连即可，**不需要代理**（`SCAN_PROXY` 默认空）。本机（中国大陆）跑要 `export SCAN_PROXY=http://127.0.0.1:12334`
- 计划每 4 小时跑一次 `pool.py`（cron，尚未挂）

```bash
cd ~/funding-arb
./.venv/bin/python scan.py
./.venv/bin/python verify.py [币名]
./.venv/bin/python pool.py
./.venv/bin/python trade_a.py open ZEC 1000     # 空跑；加 --live 才真下单
```

---

## 关键决策及依据

### 为什么从头写而不是套现成仓库
GitHub 上的要么是纯扫描器（aoki-h-jp 那个 298 星的 README 直接写明不含自动套利），
要么主打链上 DEX（50shadesofgwei）跟需求不对口。难的部分——两腿对冲、单腿回滚、
对账、风控——跟具体账户和阈值绑死，终究得自己写。

### 为什么选模式 A，放弃跨所（模式 C）
数据说话（2026-09-07，清洗后）：

- **跨所最好**：ARB hyperliquid→bybit 净年化 5.9%，24 个主流币配对里 0 个够到 8% 开仓线
- **模式 A 最好**：ZEC hyperliquid 9.1%、ETH hyperliquid 8.2%、BTC hyperliquid 7.3%、
  DOGE mexc 7.0%、LINK phemex 6.1%、SUI bybit 5.4%、ZEC okx 4.5%

模式 A 不只是收益高，风险结构也更好：**只在一个所里操作**——不用两边各备保证金、
仍可能发生单腿失败、部分成交、断网或平仓受限。同所操作不等于两腿原子成交。

有意思的是模式 A 前三名全在 hyperliquid，跟四周验证一致（hyperliquid 费率系统性高于大所）。
**模式 A 在 hyperliquid 上做 = 吃同一个已验证的溢价，但不用跨所。**

### 为什么第一版做 okx 而不是收益更高的 hyperliquid
第一版目的是验证链路（开两腿→收费率→记账→平仓）能不能自动跑，收益次要。
okx 已有账户、API key 五分钟、ccxt 的 CEX 下单接口成熟、现货深度大（ZEC 现货 113M）。
hyperliquid 要配钱包私钥、资金桥过去、下单接口跟 CEX 不一样。链路稳了再接 hyperliquid 吃那 9%。

### 为什么砍掉 blofin（它本来是最好的）
blofin 的费率偏差是全场最大且四周稳定（中位 17.7/19.9/16.4/13.2，比 binance 高 8~12 点）。
但它的公开行情接口被 Cloudflare 挡：**403 + "Just a moment..."**，换浏览器 UA 也过不去
（ccxt issue #27555 同样问题）。**不是香港被封**——blofin 的受限名单里没有香港，
而且同一台 VPS 上其它 14 家全部正常。

砍掉的理由不是连不上不方便，而是：**开得进、平不出的场子会让你锁仓**。
费率翻向时跑不掉，比少赚几个点严重得多。

### 收益门槛
下限 5% 年化，上不封顶。`pool.py` 的开仓线设在 8%（留安全边际），平仓线 3%。

---

## 数据陷阱清单（这些坑全都踩过，别再踩）

裸调 ccxt 拿到的数据有一堆陷阱，每一条都会让你算出好看但假的年化。

**1. 结算周期交易所会报错。** binance 的 `fetch_funding_rates` 走 premiumIndex 接口，
不返回 `fundingIntervalHours`，ccxt 给 `None`。按 8 小时兜底而实际是 4 小时，年化就少算一半。
→ 现在用**历史时间戳的间隔中位数**倒推真实周期，发现被 limit 截断还会补拉一次。

**2. 合约的 `baseVolume` 是张数不是币量。** 要乘 `contractSize`。mexc 的 BTC 一张 0.0001 币，
不乘虚高一万倍——曾经算出 22 万亿美元的日成交额。`quoteVolume` 各所口径也不统一
（okx 返回的是币量不是美元）。→ 现在两种算法都跑，**取小的**。

**3. 瞬时费率没有意义。** 单期费率乘 1095 折出来的「年化 500%」满地都是，7 天一平掉到几十。
→ 用 7 天均值，并看**同号占比**（过去 7 天费率保持同方向的期数比例）。50% 等于抛硬币，
开仓吃两期就被反向费率吐回去还赔手续费。

**4. 新上市合约的「7 天均值」可能只有 4 期数据。** → `MIN_HIST_DAYS = 5`。

**5. 币名不等于合约。** 同一个所的 BTC 可能有 USDT/USDC/USD1 好几种本位，按币名配对会把
USDC 合约和 USDT 合约当成一个东西。→ 按 `(币, 本位)` 区分。

**6. 有些所的费率是假的。** bingx 一半以上合约的 `lastFundingRate` 直接返回默认值 0.0001，
37 行里 23 行年化一模一样。→ 已移除。

**7. 现货「存在」不等于「能成交」。** 模式 A 一度只检查 ccxt 市场列表里有没有这个现货，
没检查它有没有量。加上现货量筛后，4.5% 以上从 25 行掉到 7 行——**一半的模式 A 候选是假的**。
BTC backpack 8.5%、XLM/UNI/LINK/SUI/AVAX/LTC/ADA/AAVE 在 mexc 上的那批全部消失。
→ 现在现货腿也要 ≥ `MIN_VOL_USD`。

**8. 分页：`since` 是从旧往新给的。** 1 小时结算的币 28 天有 672 期，单次返回覆盖不到，
不分页就只拿到最老的几周，**最近一周反而是空的**。`verify.py` 一度因此把 hyperliquid
误排到第二名（那个 77.2% 是单个币算出来的）。→ `pull_history()` 分页拉满。

**9. 负费率的机会大多够不着。** 榜首经常是 -1000% 这种，但负费率要收就得做多永续+做空现货，
需要借币。这些币借不到或借币利率比费率还高。**能做的正费率里超过 50% 的很少。**

**10. 场子中位数 ≠ 能吃到的价差。** blofin 中位数 17.7%，可它的 BTC 是 -6.8%，同一家里差 24 个点。
拿中位数当交易依据会亏钱。→ `verify.py` 第三张表按**最差一周**筛具体币对具体所。

---

## 已验证 vs 未验证

**已验证（跑过真实数据）**
- 16 家所的抓取、清洗、成交量换算、周期推断 — 多轮修正后数据可信
- 费率偏差的稳定性 — 28 天按周切片，blofin/hyperliquid 的溢价四周都成立
- `pool.py` 的开/持/平决策逻辑 — mock 覆盖了滞后带、最短持仓、白名单三条路径
- VPS 上 14/16 家所直连正常（woofipro、bitmex 长期 0 行，量筛不过）

**未验证（接手要特别小心）**
- **`trade_a.py` 从没碰过真实 okx 账户。** 逻辑用 mock 覆盖了空跑、单腿回滚、
  敞口残差、平仓四条路径，但这些只有真下单才知道：
  - okx 持仓模式是净持仓还是双向
  - `tdMode` 要不要显式传
  - 统一账户里现货和合约的资金要不要划转
  - 市价单的实际滑点
  → **第一笔必须小额（200U 量级）**，开完去网页确认两腿都在、净敞口接近零，再走一次平仓。
- 换仓频率对实际收益的影响 — `pool.py` 还没连续跑过，`pool_log.csv` 是空的
- hyperliquid 现货的**盘口深度**（只看了日成交额 13~23M，没看订单簿）

---

## 下一步

1. `trade_a.py` 小额实盘，跑通开仓→平仓，确认 okx 接口行为
2. 挂 cron 让 `pool.py` 每 4 小时跑，攒 `pool_log.csv`，一两周后看真实换仓频率
3. 链路稳了再考虑：接 hyperliquid（收益 9% vs okx 4.5%）、放开 meme 池（`MAJORS_ONLY = False`）

## 参数速查（都在文件顶部）

| 参数 | 默认 | 含义 |
|------|------|------|
| `MIN_VOL_USD` | 1000 万 | 24h 成交额门槛，永续和现货腿都要过 |
| `LOOKBACK_DAYS` | 7 | 费率均值回看天数 |
| `MIN_HIST_DAYS` | 5 | 历史不足不进榜，挡新上市合约 |
| `MIN_SAME_SIGN` | 85% | 费率方向稳定性下限 |
| `MIN_SPREAD_APR` | 10% | 跨所价差年化下限（scan.py 的表） |
| `HOLD_DAYS` | 30 | 摊薄开平成本用的假设持仓天数 |
| `MAJORS` / `MAJORS_ONLY` | 32 币 / True | 币池白名单 |
| `STABLES` | USDT/USDC/USD | 认哪些稳定币做保证金 |
| `ENTRY_NET_APR` / `EXIT_NET_APR` | 8% / 3% | pool.py 开仓/平仓线，**中间的滞后带是防来回换仓的**，别设成一样 |
| `MIN_HOLD_DAYS` | 3 | pool.py 最短持仓，同上 |

**换仓成本要记牢**：四笔 taker 约 0.22%，按 30 天持仓摊薄是 **2.7 个点年化**。
一周换一次就是 11.4 个点，比赚的还多。所有阈值设计都是围绕这个数。

## 安全

- okx API key **只开交易权限，禁用提现**
- key 走环境变量 `OKX_API_KEY` / `OKX_SECRET` / `OKX_PASSWORD`，不入库
- `trade_a.py` 默认空跑，必须显式加 `--live` 才下单
