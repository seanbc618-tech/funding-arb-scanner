# 全自动资金费率套利 Bot 实施方案

版本：v1.0，2026-09-08。状态：用户已审阅并批准按顺序实施；F01—F05 已获准继续；F06 原版已上线；F01—F06 复核发现 5 处问题，已本地修复并通过 198 项检查，修补已于 2026-09-09 全部部署，详见末尾部署记录。

本文完整承接上一轮已获用户认可的项目分析，并细化为可逐项实施、展示与验收的任务。本文中的功能、文件和状态设计是目标方案，不代表已经实现或实盘验收。

## 1. 本次协作规则与停止点

用户明确要求：先把全部分析写入实施文档；文档写完先停，用户看完后再继续。后续按顺序一个功能一个功能实施，每完成一个功能必须停下来，让用户审阅后再进入下一项。

执行约定：

1. 本轮只新增本文，不修改业务代码，不启动新功能，不改变远端服务，不提交、推送或部署。
2. 文档获准后，从任务 F01 开始。第 9 节每个任务 ID 是一个独立审阅单位，不把整阶段合并成一次交付。
3. 每项先检查实际文件、未提交变更和运行状态；保留用户修改、持仓、订单、账单和历史实验产物。
4. 只实施当前任务及其必需依赖。发现依赖未完成或会改变已确认规则，说明具体影响，更新方案供审阅，不悄悄扩大范围。
5. 每项完成必要验证后，给用户展示功能、证据、限制和变更清单，更新任务记录，然后停止。用户确认继续后才进入下一项。
6. 文档认可不等于授权真实交易。功能开发、部署启用、真实账户接入与实盘资金额度分别按当时明确授权处理；已经确认的授权不重复询问。
7. 逐项审阅能减少遗漏，但不能保证绝无错误；必须依靠可复现检查、异常场景和实际对账证据验证。

## 2. 项目定位与总体方向

当前项目是“研究扫描器＋执行原型＋只读监控”。主要缺口是交易全流程衔接、异常恢复和组合管理。已有模块雏形不能等同于无人值守标准。

目标是一个可持续运行的全自动资金费率套利 Bot：自动发现机会、分配资金、建立双腿对冲、监控持仓、采集真实资金费、核算收益、自动退出和换仓，并通过 Telegram 展示状态与告警。

首版保留 Python＋ccxt，聚焦 OKX 模式 A：同所买现货、做空对应线性永续。ccxt 负责交易所接入；交易意图、执行恢复、资金安全、风险决策和收益归属由本项目负责。

先把一个交易所、一种策略的完整闭环做可靠，再扩展其他交易所和跨所策略。暂不整体替换为大型框架，不通过拼接多个开源机器人构造执行系统。

最终产品必须能够解释：持有什么、为什么持有、真实收到多少资金费、现在退出成本多少、风险是否越线、为什么换仓或继续持有。

## 3. 现有能力与缺口

以下依据上一轮本地源码检查。远端服务状态和外部仓库版本为当时证据，本轮仅写文档，未重新验证远端账户或运行状态。

| 环节 | 当前实际能力 | 自动运行还缺什么 |
|---|---|---|
| 机会扫描 | 多交易所资金费率、历史稳定性、成交量过滤 | 按实际投入金额计算净收益排名；持续行情存储；数据源健康状态 |
| 开平仓 | OKX 双腿 IOC、客户订单号、实际成交核验 | 自动处理部分成交、单腿失败、进程中断后的恢复 |
| 风险检查 | 深度、基差、敞口、保证金率、清算距离 | 独立持续检查、组合限额、风险处置优先级 |
| 持仓管理 | 单次 HOLD/EXIT 建议；显式允许后平仓 | 自动开仓、资金分配、换仓、冷却期、失败后的继续处理 |
| 收益记账 | 成交及账单证据、平仓后核算 | 持续增量采集、持仓期间收益、资金占用、组合净值 |
| 常驻监控 | 每分钟查询并写 JSON | 完整对账、异常告警、进程失联告警 |
| Telegram | 本机按次发送 | VPS 自主发送、交易事件通知、定期持仓报告 |
| 模拟验证 | 干跑和临时回归用例 | 真实行情驱动的模拟成交、资金费结算、完整换仓周期 |

### 3.1 现有模块的去留

| 文件 | 后续角色 |
|---|---|
| `scan.py` | 继续提供行情与候选过滤；逐步把扫描与最终开仓决策分开 |
| `verify.py` | 保留历史分周验证；用于研究证据，不直接触发交易 |
| `pool.py` | 保留模式 C 跨所研究；其开/持/平思路可参考，但不冒充模式 A 执行主循环 |
| `trade_a.py` | 保留订单意图落盘、IOC、实际成交与余量核验，补齐执行生命周期 |
| `check_a.py` | 拆清风险判断和可成交性判断，供主循环及监控复用 |
| `manage_a.py` | 保留单次检查入口，逐步复用统一管理决策 |
| `accounting.py` | 发展为持续证据采集和可追溯收益核算 |
| `monitor_a.py` | 修正对账，显示新鲜度、风险和归属状态 |
| `notify_run.py` | 保留人工按次通知；与 VPS 后台事件通知共享消息格式或发送适配 |
| `funding-arb-monitor.service` | 保留只读服务的运维方式；后续执行服务单独明确权限与职责 |

### 3.2 必须先处理的已知问题

**A. 监控对账漏报与误报。** `monitor_a.py:compare()` 只比较永续张数，没有校验方向和现货对冲腿。上一轮最小复现：本地预期空头 2 张，实际为多头 2 张，返回无差异；本地已平仓且为零张、账户无仓，反而返回 `LOCAL_POSITION_NOT_FOUND`。需要检查方向、实际数量、现货归属及历史关闭状态；未知数量不能当零。

**B. 盘口失败会提前中止后续风险检查。** `check_a.py:check()` 先检查盘口，再检查保证金和清算距离。盘口不足可能直接返回 BLOCK，账户风险尚未评估。持仓检查必须分别报告“危险程度”和“当前能怎样退出”，独立采集可取得的风险证据。

**C. 执行恢复仍需人工触发。** `trade_a.py` 已保存 pending 与确认余量，`recover` 查询原单的原则正确，但没有主循环负责接管未完成动作。需要区分状态未知和状态已确认后可继续处理的余量。

**D. 长期记账断点。** `accounting.py:collect()` 从开仓时间重新查询，超过保守 89 天窗口直接报告缺口。需要持续增量采集并使用已有本地证据；不能仅因当前接口不再返回历史就丢弃已经真实采集的账单，也不能补造原本未采集的历史。

**E. 运行与通知尚未闭环。** 上次远端证据为服务 active/enabled、linger=yes，但缺 OKX 凭据，快照为 `CONFIG_MISSING`。该状态不能解释为账户无仓。Telegram 只有本机按次发送，并非远端持仓变化自动通知。

### 3.3 保留现有安全语义

- dry/live 状态隔离，旧状态不能自动转成实盘仓位。
- 下单前保存唯一订单意图；订单提交响应不等于成交。
- 按确认成交、费用和余量核验；未知订单先查询，不盲目重发。
- 真实动作需要明确的实盘模式与额度；不自动改变账户模式、杠杆或划转资金。
- 已有 53 个临时回归用例的历史通过记录只覆盖当时场景，不是本方案整体通过证明。关键新增场景需要可复现、可维护的验证证据。
- `HANDOFF.md` 中存在不同时点描述，后续更新应标清历史快照与当前事实。

## 4. 开源仓库参考与复用计划

上一轮已将以下 8 个仓库拉取至临时目录，阅读核心实现与许可材料，未运行外部交易程序。“小模块复用候选”不代表允许直接运行完整机器人，正式取用仍需适配和验证。

| 仓库 | 已检查版本 | 分类 | 值得取用的部分 | 约束与判断 |
|---|---|---|---|---|
| [hamood1337/CryptoFundingArb](https://github.com/hamood1337/CryptoFundingArb) | `100c67d` | 小模块复用候选 | `funding.py`、`fees.py`、`symbols.py` 的单位处理、费用模型及测试案例 | MIT；包含 Kraken 等扫描；不开自动交易；净 APR 不含滑点、借贷和双腿资金占用，不能直接作为最终开仓依据 |
| [aoki-h-jp/funding-rate-arbitrage](https://github.com/aoki-h-jp/funding-rate-arbitrage) | `cca3531` | 参考 | 交易所接口示例、资金费与手续费查询 | MIT；明确不自动交易；所查 HEAD 为 2023 年提交，与现有扫描器重叠较多 |
| [Birantx/deltafarm](https://github.com/Birantx/deltafarm) | `41da625` | 重点借鉴，部分提取 | 成本覆盖判断、paper adapter、风险限额、kill switch 验证方法 | MIT；HL 专用；默认 0.02% builder 费用；订单重发假设不能直接移植 OKX |
| [Xtley001/osprey](https://github.com/Xtley001/osprey) | `215e34f` | 重点借鉴策略与界面 | 收益持续性、组合分配、换仓回本时间、持仓展示 | MIT；TypeScript；部分损失判断仅看手续费与 funding，不能充当包含基差损益的总风险指标 |
| [cgaspart/HL-Delta](https://github.com/cgaspart/HL-Delta) | `9dd3fcf` | 参考流程，不照搬执行 | 自动选币、建立对冲仓、费率下降后换仓 | README 含 MIT 文本；订单查不到或非 open 被当作成交，不能用于我们的成交确认 |
| [vooi-app/vooi-funding-bot-example](https://github.com/vooi-app/vooi-funding-bot-example) | `c3ceab2` | 重点借鉴运行与恢复 | orphan 单腿处理、状态快照、亏损冷却、实际账户核对、运行报告 | MIT；依赖 VOOI API，引入它会增加中间服务；Telegram 仍列于待扩展项 |
| [Gajesh2007/funding-arb-bot](https://github.com/Gajesh2007/funding-arb-bot) | `9b016c5` | 参考执行分层 | 双腿路由、风险管理、模拟成交、PnL 跟踪 | TypeScript＋HL/Lighter；所查版本未找到明确许可证文件，暂不复制；费用等存在固定假设，默认 dry-run 不证明实盘正确 |
| [mariamlulu/delta-basis-trading](https://github.com/mariamlulu/delta-basis-trading) | `61842ff` | 参考研究方法 | 基差回测、资金费支付、绩效报告的组织 | 不是执行机器人；README 声明 MIT，但所查版本未找到其指向的 LICENSE；结算时点和数据缺口需重验 |

### 4.1 重点吸收顺序

1. `deltafarm`：成本与模拟的设计。
2. VOOI 示例：执行恢复和运行管理。
3. `osprey`：换仓决策、组合分配与展示。
4. `CryptoFundingArb`：有明确边界的小函数和测试案例。
5. 其余仓库主要用于比较设计、核对接口和补充研究视角。

补充判断：`osprey` 当前源码包含交易和换仓代码，不能仅凭搜索页“情报基础设施”的描述排除其执行部分。也不能把其 README 的双腿“原子”描述当作交易所保证；一腿失败后的补偿平仓仍可能失败。

用户提供的调查未发现可靠 Kraken 执行机器人，本次也没有据此确认可直接使用的 Kraken 执行方案。扫描支持与执行支持必须分开记录；首版不因此扩展 Kraken 实盘。

### 4.2 复用准则

- 优先提取纯计算、数据标准化和验证案例；不要整仓拼接，也不为复用少量代码引入整套 TypeScript 或 VOOI 运行体系。
- 提取 MIT 代码时保留版权和许可声明、记录仓库 URL、来源提交、原文件、适配内容及验证结果。
- 未明确许可证的代码暂不复制；若后续需要，先确认许可。
- 不自动带入 builder 费用、推荐地址、第三方鉴权服务或默认交易参数。
- 按实际交易所核验客户订单号和重试语义；不能把某仓库“相同 ID 重发即幂等”的假设照搬到 OKX。
- 验证费用、资金费结算时点、合约张数、精度、部分成交和资金占用；第三方测试通过不能代替本项目接口契约验证。
- 星数、README 中的 production-ready、收益截图与测试数量均不是可靠盈利或无人值守证据。只做单边追资金费的策略不能作为双腿套利执行方案。

## 5. 目标运行架构与生命周期

主流程：

```text
启动对账 → 扫描候选 → 净收益决策 → 资金预留 → 双腿开仓
    ↑                                         ↓
重启恢复 ← 未完成动作记录                 持仓与风险管理
                                              ↓
                         继续持有 / 风险退出 / 普通退出 / 换仓
                                              ↓
                                成交确认 → 对账记账 → 下一轮
```

### 5.1 统一主程序

拟新增 `bot.py`，调度已有模块。启动时先核对真实账户、挂单、本地状态和未完成动作，满足条件才允许新增仓位。同一实盘账户只允许一个执行进程控制订单。

全市场扫描可以较慢；订单确认、风险检查不能被扫描阻塞。实现时依据现有依赖选择简单的分任务调度，不预先引入微服务、消息队列或大型框架。

### 5.2 可持久恢复的交易状态

每笔对冲交易有独立交易 ID，保存两腿目标数量、确认成交数量、费用、残余敞口、订单 ID、当前阶段和决策原因。可采用如下逻辑状态，具体命名在实现任务中确认：

`PLANNED → OPENING → OPEN → CLOSING → CLOSED`

异常状态至少区分 `PENDING_UNKNOWN`（订单事实未知）、`RECOVERY_REQUIRED`（存在已知未完成动作）、`PAUSED`（禁止新增动作）。任何中断都从已保存意图与交易所事实恢复，不从“上次命令退出码”推断成交。

### 5.3 权限与数据边界

paper、只读账户和 live 明确区分。风险监控发现问题不等于自动获得平仓权限。后续自动实盘只能在用户确认的币池、资金、风险与退出边界内运行。

配置缺失和数据缺失不能变成空仓或零损益。原始订单、持仓、历史状态、账单与研究产物不重置；状态迁移须有可审阅方案和备份。

## 6. 七类目标功能的完整要求

### 6.1 统一调度与启动对账

- 主循环串联扫描、决策、资金、执行、持仓管理、对账、记账与通知。
- 新开仓之前处理启动对账和未完成动作；同账户单执行者。
- 扫描失败不能阻断已有持仓的风险检查与订单确认。
- 能解释当前循环处于哪一步、上次成功时间和阻塞原因。

### 6.2 净收益选币与资金分配

排名依据从“谁的 APR 高”改为“按实际金额和持有时长，扣除全部增量成本是否值得”。至少覆盖：

- 预计资金费收入；区分历史已结算费率与当前预测费率，避免把预测当入账。
- 现货、永续分别适用的真实账户手续费。
- 买卖价差和对应下单量的盘口滑点。
- 基差变化的压力情景。
- 现货资金、永续保证金、备用金的总占用。
- 单币上限、组合总名义上限、最大持仓数和保证金储备，集中配置。
- 高费率但无法按金额成交的标的不进入执行队列；允许没有合适机会时保持现金。

净收益、总资本收益率和资金费率 APR 分别展示。总资本不明确时不补造收益率。基差压力情景与期望收益分别列示，不把压力结果误称确定预测。

### 6.3 双腿执行与异常恢复

- 保留先保存意图再提交订单的机制，成交以交易所确认事实为准。
- 覆盖已知部分成交、单腿失败、超时未知、重启恢复、精度尘埃及余量继续退出。
- 未知订单先查询；只有已确认余量才能形成下一笔动作，防止重复开仓。
- 两腿同时提交不保证同时成交；需要正式的单腿风险处置路径。
- 每次补偿或退出后重新检查实际仓位，不能因“已发出平仓请求”宣称已平。
- 自动恢复的动作范围必须和当前授权一致；状态不明时暂停新增动作并告警。

### 6.4 独立风险控制

账户危险程度与当前可成交性分别计算；盘口不足时继续采集可取得的保证金、清算距离、仓位方向等证据。

风险动作分层：禁止新开仓、暂停普通换仓、减少风险敞口、紧急退出、状态不明时暂停新增动作并告警。

组合风险包含单币和总名义上限、现金与保证金储备、方向及数量匹配、未完成订单和未归属账户仓位。风险检查应有更新时间，不能用旧快照冒充新鲜账户状态。

继续保留用户已选保守原则：数据不全暂停动作；负资金费率、保证金或清算距离越线时建议退出；执行实盘动作仍需明确 live 授权。普通换仓和紧急减仓使用不同规则，任何放宽紧急滑点等变化必须预先明确边界。风险退出优先于持有期与换仓冷却，但不能突破未授权行为或用未知状态猜测下单。

### 6.5 自动换仓

决策原则：

> 换到新仓的预计净收益 − 继续持有旧仓的预计净收益 > 换仓增量成本＋不确定性缓冲。

实现时统一会计口径：若前两项采用未扣切换费用的未来经营收益，则右侧单列切换成本；若“换到新仓的净收益”已包含这些费用，则右侧不能再次扣除。用独立字段和算例验证无重复计费。

换仓成本包括旧仓退出、新仓进入的手续费与滑点、切换期间收益影响。比较同一未来时间窗口、可用资本及预计持有时长，过去已经支付的沉没成本不重复进入是否换仓的增量比较。

算例：两仓均为 1,000 USDT 永续名义，新仓年化资金费收益比旧仓高 5 个百分点，则每日增加约 `1000 × 0.05 / 365 = 0.137 USDT`。若切换成本为 4 USDT，约需 `4 / 0.137 ≈ 29.2 天` 回本。这是说明成本约束的算术例子，不是收益预测。短期机会即使 APR 排名更高，也未必值得换。

配套规则：最短持有时间、信号连续确认、换仓冷却期、每日换仓次数上限、亏损后的冷却与新机会复核。具体阈值在对应任务中根据成本和验证证据确定，不直接复制外部仓库默认值。

首版顺序为：确认旧仓退出 → 核对释放资金 → 重新检查新机会 → 决定是否开新仓。退出未完成时不进入新仓；新机会消失时允许持币等待。换仓中断后按持久状态恢复，不重复退出或重复开仓。

### 6.6 持续记账、Telegram 与运行展示

继续使用 SQLite，不先引入大型数据库。逐步保存订单事件、成交、真实资金费、资金占用、策略决策和通知记录。

账单按游标增量采集、去重并保留来源证据；供应商迟发或更正需标记并核对。无法恢复的历史缺口保持 `INVALID_DECLARED_GAP` / null，不缩宇宙、不补造、不改冻结件。超过接口历史窗口但本地已完整采集的证据仍应可使用。

持仓与收益展示分别列出：真实入账资金费、手续费、未实现损益、已实现损益、预计退出成本、资金占用、组合净值及数据完整性。未平仓估值不冒充最终结算收益。

VPS 自主 Telegram 通知覆盖：

- 开仓完成、平仓完成、换仓原因及结果。
- 单腿异常、对账不一致、保证金风险、数据缺失与恢复。
- 定时持仓摘要、每日收益报告。
- 通知投递异常和后台失联状态。

消息中不包含 Token、私钥、服务器地址、登录用户名和原始敏感日志。复用个人 Telegram Notifier 的发送能力，私有配置单独保存；通知失败不重跑交易，未知投递不盲目重复发送。定期报告和事件告警有明确节流规则。

进程失联由进程外检查发现，因为死亡进程不能自报。systemd active 和 PID 只说明进程存在，验收还须检查快照更新、查询成功和账单推进；缺凭据明确显示 `CONFIG_MISSING`。

### 6.7 真实行情模拟与研究验证

当前 dry-run 直接更新模拟数量，不能验证成交能力。新增 paper 模式，用实际买卖盘和真实结算时点模拟手续费、部分成交、滑点、资金占用、资金费支付和完整换仓过程。

回测验证选币与换仓的经济意义；paper 验证连续运行流程；受限实盘验证真实成交与账单。三者证据不得混用。模拟不填补真实历史缺口，也不伪造真实入账。

## 7. 阶段目标与完成标准

| 阶段 | 交付内容 | 阶段完成标准 |
|---|---|---|
| 第一阶段：正确性与观测 | 修监控漏报/误报；分离风险与盘口；接 VPS Telegram；持续记账与摘要 | 能持续解释持仓、数据完整性、收益来源和告警原因 |
| 第二阶段：完整模拟闭环 | 主循环、决策、资金分配、模拟双腿成交、退出与换仓 | 完成开仓—结算—换仓—退出，重启后能正确恢复 |
| 第三阶段：受限自动实盘 | 接入 OKX 执行器；限定币池与总资金；核对真实账单 | 部分成交、断网、重启、余量处置均有可核查证据 |
| 第四阶段：效率与市场扩展 | maker、更多币种、第二交易所、跨所策略 | 每项扩展均有独立成本模型、接口能力与恢复验证 |

阶段只是目标集合，不能跳过每个功能完成后的用户审阅点。

## 8. 验收原则与重要参数

### 8.1 验收证据

- 每项检查围绕实际风险，不为简单文档或低影响改动添加无意义测试。
- 成交、对账、资金、风险、记账、换仓和恢复场景需要关键用例，记录命令、输入、预期和实际结果；外部 API 失败与代码回归分开说明。
- 对比改动前后持仓和证据文件，确认没有覆盖历史状态。
- 不仅检查程序退出码，还检查执行结果、账户事实、数据时间和经济算式。
- 仅在相关授权存在时进行远端部署或账户调用；没有实盘条件时明确停在模拟或只读验收。

### 8.2 待对应任务确定的参数

币池、总投入、单币及总名义上限、最大持仓数、保证金备用比例、轮询周期、净收益门槛、预期持有窗口、最短持有时间、换仓冷却、每日换仓次数、亏损冷却、普通与紧急退出边界、报告频率、paper 观察期、受限实盘额度。

本文不把这些未知参数填成自动生效的交易配置。现有阈值只作为现状参考，正式启用时结合真实手续费、盘口和账户模式核验。

## 9. 顺序实施任务清单

所有任务初始状态均为待实施。每行独立完成、独立展示、独立停下供用户审阅。

| 顺序 | 功能任务 | 主要内容 | 关键验收与展示 |
|---|---|---|---|
| D00 | 本实施文档 | 固化完整分析、边界、任务顺序与停止点 | 文档交用户审阅；本轮到此停止 |
| F01 | 修正持仓对账 | 方向、张数、现货归属、历史关闭记录、未知数量和未归属仓位 | 反向同张数必须告警；正常关闭不误报；缺失现货对冲可见；旧状态不变 |
| F02 | 独立风险评估 | 分离风险、盘口与数据可用性；分别返回风险与执行限制 | 盘口失败仍显示可取得的保证金/清算风险；资料未知有明确缺口 |
| F03 | 远端 Telegram 事件通知 | 复用插件发送能力；接入监控事件、节流、敏感字段白名单 | 在授权目标群验证事件；发送失败不影响交易状态；Token 不入库 |
| F04 | 资金费与成交增量采集 | SQLite 游标、去重、历史本地证据复用、迟发账单核对 | 同账单不重复；中断后续采；超过源窗口时区分已保存证据与真实缺口 |
| F05 | 持仓收益与资金展示 | 真实资金费、费用、已实现/未实现损益、退出成本、占用与净值 | 算例可复核，预计值与入账值分开；缺数据保持 null |
| F06 | 定时报表与进程失联通知 | 持仓摘要、日报、数据恢复；进程外新鲜度检查 | 进程存在但快照不推进能发现；避免重复刷屏；报告频率明确 |
| F07 | 执行状态与单账户控制 | 交易 ID、阶段、已知/未知订单、单执行者与启动对账 | 重启不会重复意图；并发执行被阻止；先处理未完成动作再开新仓 |
| F08 | 真实行情 paper 成交 | 买卖盘、数量精度、滑点、部分成交、费用和资金预留 | 买卖方向正确；不足深度不假设全成；单腿失败可复现 |
| F09 | paper 资金费结算 | 按结算时点与当时持仓记模拟支付，独立于真实账单 | 未持仓不收费；重复周期不重复结算；缺结算数据有缺口 |
| F10 | 净收益与候选排名 | 实际费用、执行价格、持有窗口、基差压力和资本口径 | 高 APR 低净收益会被淘汰；历史和预测区分；回本算式可复核 |
| F11 | 组合资金分配 | 单币/总名义/持仓数限制、保证金储备、预留释放 | 多候选不重复占用同笔资金；部分执行后资金状态准确 |
| F12 | 自动运行主循环 | 在 paper 模式串联扫描、对账、资金、执行和持仓检查 | 扫描延迟不阻断风险检查；展示每步决策和新鲜度 |
| F13 | 自动持有与退出 | 保守费率/风险规则、退出优先级、普通退出与紧急路径 | 费率转负等规则触发正确；数据缺失遵循既定边界；只操作可确认余量 |
| F14 | 自动换仓 | 增量收益、切换成本、缓冲、连续确认、冷却、次数上限 | 29 天回本示例复核；收益不足不换；旧仓未平不新开；机会消失可留现金 |
| F15 | 中断与异常恢复 | 双腿部分成交、未知订单、断网、重启、余量与换仓中断 | 已知余量可恢复；未知事实先查询；不能因退出码成功宣称平仓 |
| F16 | 策略回测与连续 paper 验收 | 基差、成本、真实结算时点、净收益及换仓频率验证 | 完整周期和恢复证据齐全；按预先约定观察期评估；不把模拟盈利当实盘结果 |
| F17 | 受限实盘准备 | 核验 OKX 只读账户事实、币池、资金、权限与退出配置 | 给出具体可审阅的启用参数；缺凭据或资金授权时保留阻塞，不自动交易 |
| F18 | 单笔受限实盘周期 | 在明确额度与授权内验证真实开仓、结算、退出 | 两腿成交、费用和真实资金费账单可对账；异常由已验证流程处理 |
| F19 | 受限实盘自动运行与换仓 | 启用已验证闭环、组合限制、常驻通知与恢复 | 全过程有订单/账单/决策证据；未达门槛不扩大资金或币池 |
| F20 | maker 执行优化 | 核算 maker 成本优势、挂单风险、成交后对冲与退出 | 节约手续费的收益覆盖挂单与对冲风险；未成交不假定成交 |
| F21 | 扩展币池 | 按合约规格、现货可用性、深度和费率资料准入 | 每币独立准入证据；不因高 APR 放宽数据要求 |
| F22 | 第二交易所接入 | 独立账户/接口能力、费用与资金模型 | 交易所专属订单、状态、费率结算及恢复规则验证 |
| F23 | 跨所套利 | 双所预置资金、费率差、对手方与稳定币风险、跨所恢复 | 双腿资金与故障场景独立验证；不默认新增自动跨所划转权限 |

F07/F08 优先建立持久状态和模拟执行能力，F15 再完整验证自动恢复；在 F15/F16 完成前不能把已有状态结构当作无人值守已验收。

## 10. 每项功能的交付模板

每次完成后，在本文任务记录或对应交付记录中填写：

1. 任务 ID、目标、实施前基线及用户确认范围。
2. 改动文件、功能前后差异、是否提取开源代码及来源。
3. 正常路径与关键异常路径的验证命令、结果和证据位置。
4. 是否访问真实账户、发送 Telegram、提交/推送、部署或产生真实订单，分别列明。
5. 已知限制、未运行检查和所需外部条件。
6. 给用户查看的具体结果：报表、决策样例、可复现差异或运行证据。
7. 当前任务状态：待审阅；下一项是什么。到此停止，等待用户继续。

记录表初始状态：

| 任务 | 实施状态 | 验证状态 | 用户审阅状态 |
|---|---|---|---|
| D00 | 文档已形成 | 已核对 11 节、8 个仓库及 F01—F23 连续任务编号 | 用户已审阅，批准实施 |
| F01 | 已完成本地实施 | 本轮复核 18 个专项用例通过 | 用户已审阅，批准继续 |
| F02 | 已完成本地实施与复审修复 | 34 个专项＋18 个 F01＋53 个既有离线回归通过 | 用户批准继续 F03 |
| F03 | 已实现并部署通知服务 | 19 个专项＋105 个回归通过；远端群消息 3629 确认 | 用户批准继续下一项 |
| F04 | 已实现并做独立只读接口验证 | 18 个专项＋124 个回归通过；24h 账户账单重复采集不重复入库 | 用户批准继续 F05 |
| F05 | 已实现并做独立只读展示验证 | 21 个专项＋142 个既有回归通过 | 用户批准继续 F06 |
| F06 | 已实现并上线定时报表/独立检查 | 25 个专项＋163 个既有检查通过；首条摘要 3633，重复执行未重发 | 待审阅 |
| F07 | 本地实施完成，未部署 | 22 个专项＋198 个既有检查＋4 个复核检查通过；复核修复 2 处问题，未部署 | 待审阅 |
| F08 | 本地实施及复核完成，未部署 | 244 项检查；2 类复核问题已修复，原版本公共盘口模拟通过 | 待审阅 |
| F09 | 本地实施及复核完成，未部署 | 268 项检查；真实公共接口及合成持仓记账/去重通过 | 待审阅 |
| F10 | 本地实施完成，未部署 | 284 项检查；真实账户费率和公共行情验证通过 | 待审阅 |
| F11 | 本地实施完成，未部署 | 301 项检查；双币预留、部分成交及释放验证通过 | 待审阅 |
| F12 | 本地实施完成，未部署 | 316 项检查；独立扫描进程与 paper 全链路离线通过 | 待审阅 |
| F13 | 本地实施完成，未部署 | 334 项检查；paper 风险/费率退出及确认余量续做通过 | 待审阅 |
| F14—F23 | 未开始 | 未运行 | 未开始 |

## 11. 首版范围与最终交付定义

首版价值在于 OKX 模式 A 的完整可靠闭环，不在于增加扫描交易所数量。暂不同时上线多交易所、多策略，不把 maker 或跨所功能作为首版前置条件。

首版完成时，应同时具备：

- 可解释的候选与净收益判断。
- 有资金上限的自动双腿开仓和退出。
- 正确的现货/永续对账及独立风险检查。
- 真实资金费持续采集、费用与收益分项展示。
- 有成本约束的自动换仓及中断恢复。
- VPS 自主 Telegram 通知与进程外失联检查。
- 可复现的 paper 全周期、异常验证与受限实盘对账证据。
- 用户可查看持仓、持有原因、真实入账、预计退出成本、风险和换仓理由。

当前“服务 active”和“CONFIG_MISSING”只证明监控运行及配置缺失，不能满足以上交付定义。实施从修正事实判断开始，逐项推进，每项完成后停下审阅。

## F01 交付记录（2026-09-08）

- 范围：按用户本次批准实施 F01；遵循逐项停止点，F02 尚未开始。
- 基线：业务代码开始时无未提交改动，方案文件原为未跟踪文件。已核对 monitor_a.py 与 trade_a.py 的本地状态语义。
- 改动：monitor_a.py 保留 baseline；比较永续方向与张数，拒绝重复行静默覆盖；正常关闭记录不认领新仓；未知/无效数量保留缺口，pending 显式告警；按 baseline + base 校验现货总余额，缺键不当零；未归属非 USDT 余额单独显示。
- 验证：`python3 checks/f01_reconciliation.py`，18/18 通过；`python3 /private/tmp/funding_arb_regression.py`，24/24 通过；`python3 /private/tmp/funding_modules_regression.py`，29/29 通过。专项脚本保存在仓库，可重复执行，未引入依赖或复制第三方代码。
- 样例：预期 short 2、实际 long 2 → POSITION_SIDE_MISMATCH；closed 且双腿为零、账户空 → NO_POSITIONS；baseline=0.1、base=0.02、账户 BTC=0.1 → SPOT_BALANCE_MISMATCH；账户缺 BTC 数值 → SPOT_BALANCE_UNKNOWN。
- 历史保护：实施前后根目录已有 JSON 文件 SHA-256 一致；不迁移或覆盖旧状态。
- 边界：仅本地离线验证；未访问真实账户、未发 Telegram、未提交/推送、未部署、未产生真实下单/撤单/划转。远端运行与凭据状态本轮未验证。
- 限制：现货只能按已有 baseline 与确认余量核对，不能证明每枚币的外部来源；关闭记录不继续认领账户资产，剩余非 USDT 余额显示为未归属，不能解释为策略收益。账户接口和本地状态不是原子快照，交易进行中可能暂时不一致；F01 仅报告、不触发修复或交易。旧 53 个回归脚本位于临时目录，持久专项验收以 checks/f01_reconciliation.py 为准。
- 当前：F01 待审阅；下一项 F02 独立风险评估，须用户确认继续后实施。

## F02 交付记录（2026-09-08）

- 用户已审阅 F01，并要求复核无问题后继续；本轮复核未发现新的阻塞问题，F01 业务代码未改。
- 基线：保留前轮 monitor_a.py、HANDOFF、checks 与未跟踪方案文件；修改前离线复现盘口超时导致低保证金风险漏报。
- 本项修改 check_a.py：风险、执行限制、数据缺口分开返回；账户/资金费率/双腿盘口逐项采集，单项失败继续其余检查。保留 action 兼容语义，风险越线但盘口或数据受阻仍为 BLOCK。
- 新增方向与重复持仓检查、关闭余量检查；标记本机采集时间与耗时，结束时复查盘口年龄。未放宽阈值、滑点或实盘权限。
- 验证：checks/f02_risk.py 28/28，checks/f01_reconciliation.py 18/18，既有两组离线回归 24/24 与 29/29，共 99 个通过；AST、git diff --check 通过。原有 21 个根目录状态/研究文件哈希一致。
- 展示：盘口超时＋保证金率 2＋清算距离 5%，旧函数风险原因为空；新函数显示 MARGIN_RATIO_LOW 与 LIQUIDATION_DISTANCE_LOW，risk=EXIT_REQUIRED、execution=BLOCKED、action=BLOCK。管理器不会因此执行平仓。
- 文件、验证命令、完整前后输入/输出、日志和哈希见 [F02 交付报告](artifacts/F02/REPORT.md)。README 与 HANDOFF 同步更新；未提取开源代码或引入依赖。
- 未访问真实账户、未发送 Telegram、未提交/推送、未部署，未产生真实下单/撤单/划转。
- 限制：接口仍同步且非原子快照，接口超时可能延长一次检查；本机采集时间不等同于源数据时间。整体超时会阻止动作，常驻并行调度留到 F12。未进行实盘或远端验收。
- 当前：F01 已审阅；F02 待审阅；下一项 F03 远端 Telegram 事件通知，按逐项规则暂停。

## 2026-09-09 复审更正（优先于前述验收结论）

用户追问可靠性后，额外复现了既有用例未覆盖的共用账户门禁漏洞：
1. 现货 total=None、本地现货余量为零但仍有永续时，trade_a.account() 的 `or 0` 将未知余额当零，F02 错误返回 EXIT / COMPLETE / READY。
2. fetch_open_orders() 返回 None 时，真假值判断将它当作无挂单，F02 错误返回 HOLD / COMPLETE / CLEAR。

修复 trade_a.py 共用账户函数：现货余额必须是明确有限非负数；挂单与持仓响应必须是列表。未知余额不再补零，明确零值仍有效。直接执行器与 F02 复用同一修复，避免仅在展示层阻止。新增 6 个测试方法（含多输入子场景），覆盖缺键、None、空字符串、NaN、两腿挂单响应、空字典持仓响应、真实零值和管理器阻止平仓。

当前验证：F02 34、F01 18、既有执行器 24、模块 29，共 105 个离线用例通过。日志为 artifacts/F02/review-*.log，修复后输出为 artifacts/F02/review-cases.json，当前代码哈希为 artifacts/F02/review-verification.json。21 个原有状态/研究文件哈希未变，AST 与 git diff --check 通过。旧日志、verification.json 与前后示例保留为原交付历史，不能代替本次代码验收。

此前“99 个通过”只覆盖原有场景，不足以证明缺数据必然阻止动作。上述两个已复现问题现已修复；仍仅达到已覆盖场景的本地离线验证，未进行真实账户、远端或无人值守验收。没有真实订单、撤单、划转、Telegram、部署、提交或推送。F02 继续待审阅，F03 未开始。

## F03 交付记录（2026-09-09）

- 用户已批准开始 F03。核对远端原服务 active、快照 CONFIG_MISSING 后实现，不将缺配置解释为无仓。
- 新增独立事件通知进程和服务，复用个人 Telegram Notifier；白名单消息、300 秒节流、跨重启去重、发送前持久化、未知投递不重试。通知不调用账户或交易入口。
- 共 124 个离线用例通过；远端向原配置群“情报小分队”发送一次实际配置缺失事件，回执 3629；重启后去重，两个服务 active/enabled。通知私有配置权限 600。
- 部署范围、版本哈希、测试日志、敏感字段边界与限制见 [F03 交付报告](artifacts/F03/REPORT.md)。旧代码备份，原有远端 3 份、本地 21 份状态/研究文件哈希未变；无 Git 提交/推送。
- 未进行真实交易、撤单、划转或 OKX 账户调用。真实持仓变化告警尚无账户条件，只做离线场景验证；trade_a.py 的本地执行门禁修复本轮未部署。
- F03 待审阅，下一项 F04；不提前实施 F06 定时报表/失联监测。

## F04 交付记录（2026-09-09）

- 用户要求解释保证金率缺失，无妨碍则继续下一项。只读确认账户汇总 mgnRatio 为空，而现有逐仓多头的仓位级 mgnRatio 有值；账户 posMode=long_short_mode，不符合执行器 net_mode 要求。未改变账户模式或接管仓位，只读采集可继续。
- accounting.py 新增持久分页、查询覆盖、七天重叠复查与不可变更正冲突记录；已完整采集的旧窗口可继续使用，无覆盖证明仍 INVALID_DECLARED_GAP/null。
- 新增 --account-sync --since 与可选 --interval，仅账户证据采集、不归属策略、不计算策略利润。
- 18 个专项＋124 个既有回归通过。真实 OKX 最近 24h 两次采集均为 1 条账单、无成交记录；原始证据保存在远端独立 600 数据库，未复制到账户外或写入仓库。
- 改动、命令、哈希与边界见 [F04 交付报告](artifacts/F04/REPORT.md)。原有本地 21 个状态/研究文件哈希不变；远端仅新增独立验证副本与证据库，未替换运行模块或启停服务。
- 未下单、撤单、划转、改变持仓模式或发送额外 Telegram；未提交或推送。F04 待审阅，F05 尚未开始。

## F05 交付记录（2026-09-09）

- 用户批准 F05，断点检查确认没有已落地 F05 文件；保留此前未提交修改。用户表示准备切换单向持仓，本轮只读查询仍返回 long_short_mode，没有替用户改变模式。
- 新增 portfolio_a.py 只读 JSON/Markdown 报表：已入账资金费、费用原币、已实现/未实现损益、退出成本与退出后估计净损益、现货成本/市值、账户 USD 净资产与保证金。缺策略资本归属时策略净值和收益率 null。
- 21 个专项＋142 个既有回归通过；现货币量手续费不重复扣除，报价完成时再核验时效，SQLite 只读且哈希不变。原有本地 21 个状态/研究文件未变。
- 真实只读展示可取得账户净资产，账户 1 个外部仓位、本策略 0 个，策略收益不伪造。完整报表仅保存在远端独立 600 目录，本地保留无余额明细摘要。
- 改动、命令、算例、哈希及限制见 [F05 交付报告](artifacts/F05/REPORT.md)；未部署替换线上业务或启停服务，未交易、划转、改模式、额外 Telegram、提交或推送。
- F05 待审阅，F06 未开始。

## F06 交付记录（2026-09-09）

- 已按默认每小时摘要、北京时间 08:00 日报部署；独立每分钟检查监控和通知心跳，180 秒陈旧阈值，300 秒变化通知冷却。
- 共 188 个检查通过；真实只读报告预检通过，Telegram 摘要回执 3633，重复触发没有重复发送。
- 日账目覆盖缺失保持 null；日级策略净收益没有完整归属/估值证据时保持 null。未新增常驻采集，未执行交易或修改账户模式。
- 实施、哈希、上线状态及限制见 [F06 交付报告](artifacts/F06/REPORT.md)。F06 待审阅，F07 未开始。

## F01—F06 综合复核（2026-09-09，优先于前述阶段性验收）

发现 5 处代码问题并完成本地修复，补齐历史测试仅存临时目录的问题，198 项检查通过。修补尚未部署，不能沿用“全部已完成”概括线上状态。具体复现、影响、修复与缺口见 [综合复核报告](artifacts/F01-F06-review/REPORT.md)。

用户因现有仓位暂不能切换单向，保留账户与仓位；门禁验证会拒绝双向模式。净值/收益率、完整日级收益与持续采集仍有明确交付边界。F07 未开始，恢复时先处理本轮只读修补上线。

## F07 交付记录（2026-09-09）

用户批准实施 F07。本地新增交易/订单身份与执行状态、账户绑定锁、启动全局对账门槛，220 项检查通过。未部署、未真实执行，账户模式不变；旧状态未迁移。上一轮只读修补仍待独立部署。范围、验收和限制见 [F07 报告](artifacts/F07/REPORT.md)，F07 待审阅，F08 未开始。

## F07 复核记录（2026-09-09）

复现并本地修复旧退出判断误用于新交易周期、历史客户订单号去重遗漏；暂停显示疑点验证正常。4 个新用例与原 220 项检查通过，共 224 项。详情见 [F07 复核报告](artifacts/F07-review/REPORT.md)。修补未部署，F08 未开始。

## F01—F07 全部更新部署（2026-09-09，覆盖历史部署状态）

用户授权全部部署，F01—F07 与两轮复核补丁已上线；VPS 224 项离线检查、真实账户只读预检及服务/哈希验收通过。未启用自动交易，未改账户模式或持仓。详见 [部署报告](artifacts/deploy-F01-F07-20260909/REPORT.md)。F08 未开始。

## F08 交付记录（2026-09-09）

独立 paper 执行入口已实现，240 项检查通过，真实公共盘口模拟开平仓通过；未部署、未真实交易。模拟费用/保证金假设与恢复边界见 [F08 报告](artifacts/F08/REPORT.md)。F08 待审阅，F09 未开始。

## F08 复核记录（2026-09-09）

修复限价取整遗漏有效深度、平仓预留遗漏同步现金流两类问题；244 项检查通过，未部署或真实交易。修复前后证据及算例见 [F08 复核报告](artifacts/F08-review/REPORT.md)。F09 未开始。

## F09 交付记录（2026-09-09）

单周期 paper 资金费结算、历史持仓回放、原子去重和缺口记录本地完成，264 项检查通过；VPS SSH 认证失败、本机接口超时，真实接口验收待补。未部署或真实交易，F10 未开始。详见 [F09 报告](artifacts/F09/REPORT.md)。

## F09 复核与联网补验（2026-09-09）

SSH 认证恢复，真实公共费率/标记价格及合成持仓记账验证通过；本轮未发现实现缺陷，268 项检查通过。未部署或真实交易，F10 未开始。详见 [F09 复核报告](artifacts/F09-review/REPORT.md)。

## F10 交付记录（2026-09-09）

独立 OKX 净收益排名入口完成，284 项检查及真实只读链路通过；预计/历史/压力分列，缺数据或深度不足不进入排名。未部署或交易，F11 未开始。详见 [F10 报告](artifacts/F10/REPORT.md)。

## F11 交付记录（2026-09-09）

组合预算与 paper 执行接入完成，301 项检查通过；原有文件不变，未部署或交易。按双腿初始名义设限，预留及过期规则见 [F11 报告](artifacts/F11/REPORT.md)。F12 未开始。

## F12 交付记录（2026-09-09）

独立扫描进程与 paper 主循环完成，316 项检查通过；状态一致性、新鲜度及异常门槛保留。未常驻运行、部署或交易，F13 未开始。详见 [F12 报告](artifacts/F12/REPORT.md)。

## F10—F12 联合复核（2026-09-09）

复现并修复排名数量/结算边界、持仓检查新鲜度、预留归属持久化共 4 项问题。322 项检查通过，未部署或真实交易，F13 未开始。详见 [联合复核报告](artifacts/F10-F12-review/REPORT.md)。

## F13 交付记录（2026-09-09）

显式 paper 自动退出开关、优先级、可选持有时限和已确认部分退出续做完成，334 项检查通过；未知事实阻断，保证金/强平模型限制见 [F13 报告](artifacts/F13/REPORT.md)。未部署或真实交易，F14 未开始。

## F13 复核记录（2026-09-09）

修复确认归零后的阶段收尾和多币退出混合状态误报，338 项检查通过。未部署，线上仍未启用自动退出；F14 未开始。详见 [F13 复核报告](artifacts/F13-review/REPORT.md)。
